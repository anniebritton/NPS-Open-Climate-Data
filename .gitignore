__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
venv/

# Local agent / editor state — should never be committed
.claude/
.cursor/
.aider*/

# Build artifacts / distributions
build/
dist/

# Generated climate data
# Raw CSVs and intermediate data/ are regenerated by the EE pipeline and
# are too large to commit (~500 MB). JSON summaries in site/public/data/
# ARE committed so the deployed site has real data without running EE in CI.
data/raw/
data/site/
data/PADUS4_1Geodatabase/
site/public/data/raw/
site/public/data/carbon.json
site/public/nps-logo.svg

# Site build output
site/dist/
site/node_modules/
site/.astro/

# Local release-prep workspace (Zenodo archive builds, draft release notes,
# anything ad-hoc handed off to a co-author). Not part of the public repo.
release/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
